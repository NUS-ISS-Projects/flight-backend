package com.webapp.flightsearch.dto;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class BookmarkDtoTests {

    // @Test
    // public void testConstructorAndGetters() {
    // Integer id = 1;
    // String flightNumber = "FL123";
    // BookmarkDto bookmarkDto = new BookmarkDto(id, flightNumber, flightNumber,
    // flightNumber, flightNumber,
    // flightNumber, flightNumber, null, null);

    // assertEquals(id, bookmarkDto.getId(), "Constructor or ID getter does not work
    // as expected.");
    // // assertEquals(flightNumber, bookmarkDto.getFlightNumber(), "Constructor or
    // // FlightNumber getter does not work as expected.");
    // }

    // @Test
    // public void testSetAndGetId() {
    // Integer id = 1;
    // BookmarkDto bookmarkDto = new BookmarkDto(null, null, null, null, null, null,
    // null, null, null); // Initially
    // // null
    // bookmarkDto.setId(id);

    // assertEquals(id, bookmarkDto.getId(), "ID setter or getter does not work as
    // expected.");
    // }

    // @Test
    // public void testSetAndGetUserName() {
    // String username = "testUser";
    // BookmarkDto bookmarkDto = new BookmarkDto(null, null, username, username,
    // username, username, username, null,
    // null); // Initially null
    // bookmarkDto.setUserName(username);

    // assertEquals(username, bookmarkDto.getUserName(), "UserName setter or getter
    // does not work as expected.");
    // }

    // @Test
    // public void testSetAndGetFlightNumber() {
    // String flightNumber = "FL123";
    // BookmarkDto bookmarkDto = new BookmarkDto(null, null, flightNumber,
    // flightNumber, flightNumber, flightNumber,
    // flightNumber, null, null); // Initially null
    // // bookmarkDto.setFlightNumber(flightNumber);

    // // assertEquals(flightNumber, bookmarkDto.getFlightNumber(), "FlightNumber
    // // setter or getter does not work as expected.");
    // }
}
